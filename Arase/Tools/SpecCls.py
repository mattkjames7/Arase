import numpy as np


class SpecCls(object):
	def __init__(self,Date,ut,Epoch,Freq,Spec,Meta=None):
		#store the input variables
		self.Date = Date
		self.ut = ut
		self.Epoch = Epoch
		self.Freq = Freq
		self.Spec = Spec
		self.Meta = Meta
		
	def Plot(self,):
		#need to add code for this method
		pass
